# private-better

To install dependencies:

```bash
bun install
```

To run the web CLI:

```bash
bun run dev:web
```

Open `http://localhost:3017` and type `help`.

To build:

```bash
bun run build:web
```
